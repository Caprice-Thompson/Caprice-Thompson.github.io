{
  "scripts": {
    "cy:run": "npx cypress run",
    "cy:open": "npx cypress open",
    "cy:percy": "percy exec --verbose -- cypress run "
  },
  "devDependencies": {
    "@cypress/browserify-preprocessor": "^3.0.2",
    "cypress": "^13.3.1",
    "cypress-cucumber-preprocessor": "^4.3.1"
  },
  "dependencies": {
    "@percy/cli": "^1.27.3",
    "@percy/cypress": "^3.1.2"
  }
}
